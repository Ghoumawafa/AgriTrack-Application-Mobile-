<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.agritrack">

    <!-- ===== PERMISSIONS EXISTANTES ===== -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Permissions pour le compteur de pas -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />


    <!-- ===== NOUVELLES PERMISSIONS POUR NOTIFICATIONS ===== -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <!-- Déclarer que l'application utilise le capteur de pas (optionnel mais recommandé) -->
    <uses-feature
        android:name="android.hardware.sensor.stepdetector"
        android:required="false" />

    <uses-feature
        android:name="android.hardware.sensor.stepcounter"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AgriTrack">

        <!-- ===== ACTIVITÉS ===== -->
        <activity
            android:name=".Activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AgriTrack.Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Activities.LoginActivity"
            android:exported="false" />

        <activity
            android:name=".Activities.SignupActivity"
            android:exported="false" />

        <activity
            android:name=".Activities.AccueilActivity"
            android:exported="false" />

        <activity
            android:name=".Activities.NotificationsActivity"
            android:exported="false" />

        <activity
            android:name=".Activities.ProfileActivity"
            android:exported="false" />

        <activity
            android:name=".Activities.TerrainListActivity"
            android:exported="false" />

        <activity
            android:name=".Activities.TerrainEditActivity"
            android:exported="false" />

        <activity
            android:name=".Activities.EquipmentListActivity"
            android:exported="false" />

        <activity
            android:name=".Activities.EquipmentScannerActivity"
            android:exported="false" />

        <activity
            android:name=".Activities.EquipmentEditActivity"
            android:exported="false" />

        <!-- ===== ACTIVITÉS ANIMAUX ===== -->
        <activity
            android:name=".Activities.Animaux.AnimalCategoryActivity"
            android:exported="false"
            android:label="Catégories d'animaux" />

        <activity
            android:name=".Activities.Animaux.AnimalListActivity"
            android:exported="false"
            android:label="Liste des animaux" />

        <activity
            android:name=".Activities.Animaux.AddAnimalActivity"
            android:exported="false"
            android:label="Ajouter un animal" />

        <activity
            android:name=".Activities.Animaux.AnimalEditActivity"
            android:exported="false"
            android:label="Modifier un animal" />

        <!-- ===== ACTIVITÉS ALIMENTATION ===== -->
        <activity
            android:name=".Activities.Animaux.FeedingDashboardActivity"
            android:exported="false"
            android:label="Tableau de bord alimentation" />

        <activity
            android:name=".Activities.Animaux.TodayFeedingsActivity"
            android:exported="false"
            android:label="Repas du jour" />

        <activity
            android:name=".Activities.Animaux.GenerateScheduleActivity"
            android:exported="false"
            android:label="Générer planning" />

        <activity
            android:name=".Activities.Animaux.FoodPlansListActivity"
            android:exported="false"
            android:label="Plans alimentaires" />

        <activity
            android:name=".Activities.Animaux.FeedingHistoryActivity"
            android:exported="false"
            android:label="Historique des repas" />

        <activity
            android:name=".Activities.Animaux.FeedingStatisticsActivity"
            android:exported="false"
            android:label="Statistiques" />

        <activity
            android:name=".Activities.WeatherActivity"
            android:exported="false"
            android:label="Météo Agricole" />
        <activity
            android:name=".Activities.Sensor.StepCounterActivity"
            android:screenOrientation="portrait"
            android:exported="false" />

        <!-- ===== SERVICES ET RECEIVERS POUR NOTIFICATIONS ===== -->
        <receiver
            android:name=".Receivers.NotificationReceiver"
            android:exported="false"
            android:enabled="true" />



        <!-- ===== MÉTA-DONNÉES POUR NOTIFICATIONS ===== -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="feeding_notifications" />

        <!-- Receiver pour redémarrage -->
        <receiver
            android:name=".Receivers.BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>
    </application>

</manifest>